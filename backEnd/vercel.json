{
    "version": 2,
    "builds": [
        {
            "src": "npm run build && node build/main",
            "use": "@vercel/node"
        }
    ],
    "routes": [{ "src": "/(.*)", "dest": "build/main.js" }]
}
