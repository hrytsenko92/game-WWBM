{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "npm run build && npm start"
        }
    ],
    "buildCommand": "npm tun start",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "build/index.js"
        }
    ]
}
